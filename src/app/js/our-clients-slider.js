// eslint-disable-next-line no-unused-vars,no-undef
const slider = tns({
  container: '.reviews-wrapper',
  items: 1,
  controls: false,
  navPosition: 'bottom',
  fixedWidth: 290,
  center: true,
  responsive: {
    1024: {
      disable: true,
    },
  },
});